# Postgres connection string. On Vercel this is injected automatically when you attach a Neon store.
# For local dev, point at the same DB the worker writes to.
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
